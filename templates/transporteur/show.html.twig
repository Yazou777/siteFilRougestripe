{% extends 'base.html.twig' %}

{% block title %}Transporteur{% endblock %}

{% block body %}
    <h1>Transporteur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ transporteur.id }}</td>
            </tr>
            <tr>
                <th>Tra_nom</th>
                <td>{{ transporteur.traNom }}</td>
            </tr>
            <tr>
                <th>Tra_description</th>
                <td>{{ transporteur.traDescription }}</td>
            </tr>
            <tr>
                <th>Tra_prix</th>
                <td>{{ transporteur.traPrix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_transporteur_index') }}">back to list</a>

    <a href="{{ path('app_transporteur_edit', {'id': transporteur.id}) }}">edit</a>

    {{ include('transporteur/_delete_form.html.twig') }}
{% endblock %}
